---
interface Props { eyebrow: string; title: string; description: string; }
const { eyebrow, title, description } = Astro.props;
---
<header class="platform-page-intro"><a class="platform-breadcrumb" href="/">LLMHello /</a><span class="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></header>
